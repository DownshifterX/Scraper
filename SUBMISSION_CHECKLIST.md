# Submission Checklist

- [x] Product search works (real-time query over INE mock store catalog)
- [x] Product tracking works (saves tracked products with status, URL, name)
- [x] Current price scraping works (overcomes cookie overlay, dwell gating, WebAssembly challenge session, and honeypots)
- [x] Stock scraping works (extracts accurate real-time inventory count)
- [x] 2-hour scheduling configured (external cron trigger via `/api/cron/scrape`)
- [x] Retry logic works (up to 3 retries with linear backoff)
- [x] Slow response handling works (25s navigation timeout and 15s quote polling)
- [x] Failure handling works (logs error to audit table without corrupting history)
- [x] Invalid data rejected (strict numeric check > 0, filters strike-through MRP)
- [x] Price history works (tracked and graphed on product detail view)
- [x] Stock history works (recorded per scrape cycle)
- [x] Scrape logs work (captures duration, status, attempt, error message)
- [x] Headed scraper works (`npm run scrape:headed` launches visible Chromium demo)
- [x] README complete (clear setup instructions and architecture details)
- [x] DESIGN_NOTE complete (in-depth analysis of INE store mechanics and defenses)
- [x] .env.example complete (all required keys documented)
- [x] In-memory database fallback (application runs immediately out-of-the-box even without Supabase setup)
- [x] Only INE mock store scraped (`https://demo.inelabteamdev.com`)
- [x] No secrets committed
- [ ] Backend deployed on Render (Deploy repository `backend` directory)
- [ ] Frontend deployed on Vercel (Deploy repository `frontend` directory)
- [ ] Supabase configured (Run `schema.sql` in Supabase SQL Editor and set backend `.env`)
- [ ] cron-job.org configured (Point to `https://<render-backend>/api/cron/scrape`)
- [ ] 2–4 minute recording prepared (Demonstrate UI and `npm run scrape:headed`)
